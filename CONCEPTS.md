# word2vec_korean.py 원리 해설

`README.md`가 "어떻게 실행하는지"를 다룬다면, 이 문서는 "왜 이렇게 동작하는지"를 다룹니다.
스크립트에서 등장하는 개념(형태소 태그, 사용자 단어 점수, Word2Vec, PCA)을 코드와 연결해서 설명합니다.

## 1. 전체 흐름

```
문장 → [형태소 분석: Kiwi] → 토큰 리스트 → [Word2Vec 학습: gensim] → 단어 벡터 → [유사도 / PCA 시각화]
```

컴퓨터는 "친구"와 "학교"가 비슷한 맥락에서 쓰인다는 걸 문자 그대로는 모릅니다.
그래서 먼저 문장을 의미 단위(형태소)로 쪼갠 뒤, 각 단어가 등장하는 문맥 패턴을 벡터(숫자 배열)로 압축해서
"어떤 단어들이 비슷한 문맥에서 쓰이는가"를 계산 가능하게 만드는 것이 이 스크립트의 목적입니다.

## 2. 형태소 분석과 품사 태그 (NNG, NNP, ...)

### 형태소란

형태소는 의미를 가지는 가장 작은 말의 단위입니다. "학교에서"는 "학교"(장소, 명사)와 "에서"(조사)로 나뉘는데,
"에서"는 그 자체로 뜻을 갖는다기보다 문법적 역할(위치를 나타냄)을 합니다.
Word2Vec처럼 "단어의 의미"를 다루는 작업에서는 이런 문법 형태소(조사, 어미)가 오히려 노이즈가 됩니다.
"학교에서", "학교는", "학교를"을 각각 다른 단어로 취급하면 "학교"라는 개념이 여러 갈래로 흩어져 학습되기 때문입니다.
그래서 형태소 분석으로 먼저 쪼갠 뒤, 의미를 담는 형태소만 골라 씁니다.

### Kiwi의 품사 태그 (세종 태그셋 기반)

`kiwipiepy`(Kiwi)는 국립국어원 세종 품사 태그 체계를 기반으로 각 형태소에 태그를 붙입니다.
스크립트를 테스트하며 실제로 등장했던 태그를 기준으로 정리하면 다음과 같습니다.

| 대분류 | 태그 | 의미 | 예시 |
|---|---|---|---|
| 체언 | `NNG` | 일반 명사 | 학교, 친구, 책 |
| 체언 | `NNP` | 고유 명사 | 챗지피티, 프레임브레이크 |
| 체언 | `NNB` | 의존 명사 | 것, 수, 때 |
| 체언 | `NP` | 대명사 | 우리, 나, 그 |
| 체언 | `NR` | 수사 | 하나, 둘째 |
| 용언 | `VV` | 동사 | 가다, 만나다, 읽다 |
| 용언 | `VA` | 형용사 | 많다, 즐겁다 |
| 용언 | `VX` | 보조 용언 | (놀아)지다, (해)보다 |
| 용언 | `VCP` | 긍정 지정사('이다') | 이름이(다) |
| 수식언 | `MM` | 관형사 | 이, 그, 새 |
| 수식언 | `MAG` | 일반 부사 | 매일, 열심히 |
| 조사 | `JKS` `JKO` `JKB` `JX` 등 | 주격/목적격/부사격/보조사 | 이/가, 을/를, 에서, 는 |
| 어미 | `EP` `EF` `EC` `ETN` `ETM` | 선어말/종결/연결/명사형/관형형 어미 | -었-, -다, -고, -기, -ㄴ |
| 접사 | `XSV` `XSN` `XSA` | 동사/명사/형용사 파생 접미사 | (공부)하다의 '하'  |
| 기호 | `SF` `SN` `SL` 등 | 문장부호, 숫자, 외국어 | . , 123, ABC |

`word2vec_korean.py`의 `DEFAULT_POS`는 이 중 의미를 담는 "내용어(content word)"만 고릅니다.

```python
DEFAULT_POS = {"NNG", "NNP", "VV", "VA", "MAG"}
```

즉 명사(일반/고유), 동사, 형용사, 부사만 남기고 조사·어미·접사는 버립니다.
"학교에서 친구를 만났다"를 분석하면 `학교, 친구, 만나`만 남는 식입니다 — "만나다"의 어간 "만나"만 남고
어미 "-았-", "-다"는 제거됩니다. 이렇게 해야 "만났다", "만나서", "만나니까"가 전부 같은 단어 "만나"로 통일되어
Word2Vec이 문맥 패턴을 하나로 모아 학습할 수 있습니다.

## 3. 사용자 단어와 "점수(score)"의 의미

`--user-words`, `--user-dict`로 등록하는 신조어에는 `score`라는 값이 있습니다. Kiwi 공식 문서의 설명은 다음과 같습니다.

> 해당 형태에 부합하는 형태소 조합이 여러 개 있는 경우, 이 점수가 높은 단어가 더 우선권을 가집니다.

Kiwi는 문장을 분석할 때 가능한 모든 형태소 분리 방법(후보)을 만들고, 각 후보에 통계적 언어모델 기반 점수를 매긴 뒤
가장 점수가 높은 조합을 정답으로 선택합니다(비터비 알고리즘과 유사한 탐색). 예를 들어 "갓생살기"는

- 후보 A: `갓`(MAG) + `생`(NNG) + `살`(VV) + `기`(ETN)
- 후보 B: `갓생`(사용자 등록 NNG) + `살`(VV) + `기`(ETN)

두 가지로 쪼갤 수 있습니다. 기존 사전에는 "갓생"이 없으므로 후보 A만 존재하고, 그게 기본 결과가 됩니다.
여기에 `add_user_word("갓생", "NNG", 0.0)`로 후보 B를 사전에 새로 추가하면, 이제 두 후보가 경쟁하게 되고
`score`가 이 경쟁에서 가중치로 작용합니다.

- `score = 0.0` (기본값): 다른 후보들과 동등한 조건에서 경쟁. 언어모델 통계상 "갓생"이 자연스러우면 대개 이긴다.
- `score`를 높이면: 다른 후보가 통계적으로 더 그럴듯해도 "갓생" 쪽을 강제로 우선시한다.
- `score`를 낮추면(음수): 이 신조어가 다른 해석과 애매하게 겹칠 때 오작동을 줄이고 싶을 경우 쓴다.

실무적으로는 대부분 `0.0`으로 두고, 등록했는데도 원하는 대로 분리되지 않을 때만 점수를 조금씩 올려보는 식으로 씁니다.

```python
kiwi.add_user_word("갓생", "NNG", 0.0)
```

## 4. Word2Vec의 원리

### 분포 가설(distributional hypothesis)

"단어의 의미는 그 단어가 등장하는 문맥으로 알 수 있다"는 언어학적 가정입니다.
"학교"와 "학원"은 주변에 "다니다", "가다", "선생님" 같은 비슷한 단어들과 함께 등장할 가능성이 높으므로,
두 단어의 벡터도 비슷하게 학습됩니다. Word2Vec은 이 가설을 신경망으로 구현한 것입니다.

### CBOW vs Skip-gram (`--sg` 옵션)

Word2Vec은 "주변 단어로 가운데 단어를 맞히기(CBOW)" 또는 "가운데 단어로 주변 단어를 맞히기(Skip-gram)" 라는
가짜 예측 문제를 풀면서 그 과정에서 부산물로 단어 벡터를 얻습니다. 정답을 맞히는 것 자체가 목적이 아니라,
맞히려고 애쓰는 과정에서 비슷한 문맥의 단어들이 비슷한 벡터를 갖게 되는 것이 핵심입니다.

- `--sg 0` (CBOW): 주변 단어들 → 가운데 단어 예측. 학습이 빠르고 자주 나오는 단어에 유리.
- `--sg 1` (Skip-gram, 기본값): 가운데 단어 → 주변 단어들 예측. 느리지만 드물게 나오는 단어도 비교적 잘 학습.

이 스크립트처럼 문장 수가 적어 각 단어의 등장 횟수도 적은 경우, 일반적으로 Skip-gram이 더 안정적입니다.

### 주요 하이퍼파라미터

| 옵션 | 의미 |
|---|---|
| `--vector-size` | 단어 하나를 표현하는 벡터의 차원 수. 크면 표현력은 늘지만 데이터가 적으면 과적합/노이즈가 커짐 |
| `--window` | "주변 문맥"으로 볼 범위. 앞뒤 몇 단어까지 같은 문맥으로 볼지 |
| `--min-count` | 이 횟수 미만으로 등장한 단어는 학습에서 제외(희귀 단어 노이즈 제거) |
| `--epochs` | 전체 문장을 몇 번 반복 학습할지 |

### 유사도는 어떻게 계산하나 (`most_similar`)

학습이 끝나면 각 단어는 `--vector-size` 차원의 벡터가 됩니다. 두 단어의 유사도는 두 벡터 사이의
**코사인 유사도**(cosine similarity)로 계산합니다 — 벡터의 길이가 아니라 "방향"이 얼마나 비슷한지를 봅니다.
`model.wv.most_similar("친구")`는 "친구" 벡터와 방향이 가장 가까운 벡터들을 찾아 점수(−1~1) 순으로 반환합니다.
점수가 높을수록(1에 가까울수록) 문맥상 비슷하게 쓰였다는 뜻입니다.

## 5. PCA 시각화 원리 (`--plot`)

단어 벡터는 보통 50~300차원인데, 사람은 2차원 평면 그래프만 볼 수 있습니다.
`plot_embeddings`가 사용하는 PCA(주성분 분석)는 고차원 벡터들을 "가장 정보 손실이 적은 2개의 축"에
투영(project)해서 차원을 줄이는 방법입니다.

```python
def pca_2d(vectors: np.ndarray) -> np.ndarray:
    centered = vectors - vectors.mean(axis=0)
    _, _, vt = np.linalg.svd(centered, full_matrices=False)
    return centered @ vt[:2].T
```

동작 순서:

1. 모든 벡터의 평균을 빼서 원점 중심으로 옮긴다(`centered`).
2. SVD(특이값 분해)로 데이터가 가장 많이 퍼져 있는(분산이 큰) 방향들을 순서대로 찾는다(`vt`의 각 행).
3. 그중 분산이 가장 큰 두 방향(`vt[:2]`)에 데이터를 투영해서 2D 좌표를 얻는다.

즉 그래프에서 가까이 있는 두 단어는 원래 고차원 공간에서도 어느 정도 비슷한 벡터를 가졌을 가능성이 높지만,
2차원으로 눌러 담는 과정에서 정보가 일부 손실되므로 "그래프상 거리 = 실제 유사도"로 100% 믿으면 안 됩니다
(정확한 유사도 비교는 `most_similar`처럼 원래 차원에서 계산한 값을 봐야 합니다).

## 6. 함수별 실행 순서 (코드를 따라가며 읽기)

`main()`이 실제로 호출하는 순서대로 나열하면 다음과 같습니다. 데이터가 어떤 모양으로 바뀌며 넘어가는지 같이 봅니다.

1. **`load_sentences(args.input)`** — 파일 또는 `SAMPLE_SENTENCES`에서 문장을 읽어 `["문장1", "문장2", ...]` 형태의 문자열 리스트를 만듭니다.
2. **`build_tokenizer(args.user_words, args.user_dict)`** — Kiwi 인스턴스를 만들고, `--user-words`/`--user-dict`로 넘어온 신조어를 이 시점에 사전에 등록합니다. 그 뒤 `tokenize(문장) -> [토큰, ...]` 형태의 함수를 반환합니다. kiwipiepy가 없으면 `None`을 반환하고, `main()`은 이를 감지해 공백 분리(`simple_tokenize`)로 대체합니다.
3. **`tokenize_sentences(sentences, tokenizer)`** — 문장 리스트의 각 문장에 2번의 `tokenize`를 적용해 `[["학교", "친구", "만나"], ["커피", "마시", ...], ...]` 같은 이중 리스트를 만듭니다. gensim이 요구하는 입력 형태가 바로 이것입니다.
4. **`train_word2vec(...)`** — 이 이중 리스트를 gensim `Word2Vec`에 넣고 학습시켜, 단어별 벡터를 담은 모델(`model.wv`)을 얻습니다.
5. **`report(model, args.words, args.top_n)`** — 학습된 벡터끼리 코사인 유사도를 계산해 결과를 콘솔에 출력합니다.
6. **(선택) `model.save(args.save)`** — 나중에 재사용할 수 있도록 모델을 파일로 직렬화합니다.
7. **(선택) `plot_embeddings(...)`** — 벡터들을 `pca_2d`로 2D로 축소한 뒤 matplotlib으로 산점도 이미지를 저장합니다.

즉 이 스크립트 자체는 "새 개념을 발명"하는 게 아니라, 이미 잘 만들어진 두 라이브러리
(형태소 분석기 `kiwipiepy`, 임베딩 학습기 `gensim`)를 순서대로 파이프라인처럼 연결해주는 역할을 합니다.

## 7. Word2Vec·형태소 분석이 실제로 쓰이는 곳

이 스크립트에서 다루는 두 기술은 장난감 예제가 아니라 실제 서비스에서 흔히 쓰입니다.

**형태소 분석(POS tagging)** — 한국어를 다루는 거의 모든 NLP 파이프라인의 첫 단계입니다.
- 검색엔진 색인: "학교에서"를 그대로 색인하면 "학교"로 검색해도 못 찾으므로, 형태소로 쪼개 "학교"를 색인어로 저장
- 맞춤법/문법 검사기, 기계번역기의 전처리 단계
- 챗봇의 자연어 이해(NLU): 사용자 발화에서 의미 있는 키워드(의도·개체명)만 추출할 때

**Word2Vec류 단어 임베딩** — "단어를 벡터로 바꾼다"는 아이디어는 다음과 같은 곳에서 응용됩니다.
- 검색엔진의 질의어 확장: "노트북"을 검색해도 벡터가 비슷한 "랩탑" 관련 문서까지 함께 보여줌
- 추천 시스템(item2vec 등): 상품/영상 설명을 벡터화해 "비슷한 맥락에서 함께 소비되는" 아이템을 추천
- 감성 분석·리뷰 분석: 리뷰에서 긍정/부정과 관련된 단어들이 벡터 공간에서 뭉치는 성질을 이용
- 오탈자·유의어 사전 자동 구축: 벡터가 가까운 단어들을 유의어 후보로 뽑아 검수
- 딥러닝 모델(예: 분류기, RNN/Transformer 기반 모델)의 임베딩 레이어를 처음부터 학습시키지 않고, Word2Vec으로 미리 학습한 벡터로 초기화해서 학습 속도와 성능을 높이는 사전학습(pretrained embedding) 용도
- 문서 군집화/토픽 모델링 전처리: 형태소 분석으로 내용어만 뽑아낸 뒤, 그 벡터들을 평균 내거나 TF-IDF와 함께 문서 벡터를 구성

**사용자 사전(`add_user_word`)이 실무에서 필요한 이유** — 언어는 계속 새 단어가 생깁니다.
신조어, 게임/서비스 브랜드명, 밈(meme) 표현 등은 기존 사전에 없어서 잘못 쪼개지기 쉽습니다.
뉴스·SNS 트렌드 분석, 커뮤니티 모니터링처럼 최신 표현을 다뤄야 하는 서비스에서는
이런 사용자 사전 등록 기능이 사실상 필수입니다 — 이 스크립트의 `--user-words`/`--user-dict`가
바로 그 축소판입니다.

## 8. 정리: 왜 이렇게 설계했나

- 형태소 분석으로 조사/어미를 제거하는 이유: 같은 단어의 활용형을 하나로 묶어야 Word2Vec이 충분한 문맥 데이터를 모을 수 있음
- `NNG/NNP/VV/VA/MAG`만 남기는 이유: 이 태그들이 실질적 의미를 담는 내용어이고, 조사/어미는 문법 기능만 담당
- 사용자 단어의 `score`가 필요한 이유: 신조어는 기존 사전 단어들과 겹쳐 보이는 경우가 많아, 분석기가 어느 쪽을 우선할지 정해줘야 함
- Skip-gram을 기본값으로 둔 이유: 문장 수가 적은 소규모 데이터에서 희귀 단어도 비교적 안정적으로 학습되기 때문
- PCA로 시각화하는 이유: 추가 라이브러리(scikit-learn 등) 없이 NumPy만으로 고차원 벡터를 눈으로 확인 가능한 2D로 압축하기 위함